# JunoYi 快速参考手册

## 一、项目结构速查

```
JunoYi/
├── junoyi-dependencies      # 📦 依赖版本管理（所有版本在这里统一控制）
├── junoyi-common           # 🔧 公共工具（常量、枚举、工具类）
├── junoyi-framework/       # 🏗️ 框架封装
│   ├── framework-core      #    核心功能（异常处理、结果封装）
│   └── framework-web       #    Web功能（安全、文档、跨域）
├── junoyi-module-api/      # 📋 功能接口定义
│   └── module-xxx-api/     #    具体模块API
│       ├── domain/         #    领域对象
│       │   ├── entity/     #    实体类（数据库映射）
│       │   ├── vo/         #    视图对象（返回前端）
│       │   └── bo/         #    业务对象（业务传输）
│       ├── service/        #    服务接口
│       └── mapper/         #    数据访问接口
├── junoyi-module/          # 💼 功能实现
│   └── module-xxx/         #    具体模块实现
│       ├── controller/     #    控制器
│       ├── service.impl/   #    服务实现
│       └── convert/        #    对象转换
└── junoyi-server           # 🚀 启动入口
```

---

## 二、POM 配置速查

### 根 pom.xml 核心配置

```xml
<properties>
    <revision>1.0.0</revision>  <!-- 统一版本号 -->
</properties>

<dependencyManagement>
    <dependencies>
        <!-- 导入 dependencies 模块 -->
        <dependency>
            <groupId>com.junoyi</groupId>
            <artifactId>junoyi-dependencies</artifactId>
            <version>${revision}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

### 子模块 pom.xml 模板

```xml
<parent>
    <groupId>com.junoyi</groupId>
    <artifactId>父模块名</artifactId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
</parent>

<artifactId>当前模块名</artifactId>
<description>模块描述</description>
```

### 常用依赖引用

```xml
<dependencies>
    <!-- 公共模块 -->
    <dependency>
        <groupId>com.junoyi</groupId>
        <artifactId>junoyi-common</artifactId>
    </dependency>

    <!-- 框架核心 -->
    <dependency>
        <groupId>com.junoyi</groupId>
        <artifactId>junoyi-framework-core</artifactId>
    </dependency>

    <!-- 框架Web -->
    <dependency>
        <groupId>com.junoyi</groupId>
        <artifactId>junoyi-framework-web</artifactId>
    </dependency>

    <!-- Spring Boot Web -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- MyBatis Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-boot-starter</artifactId>
    </dependency>

    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <scope>provided</scope>
    </dependency>
</dependencies>
```

---

## 三、Maven 命令速查

```bash
# 清理并安装所有模块
mvn clean install

# 跳过测试
mvn clean install -DskipTests

# 只编译不打包
mvn clean compile

# 打包（会自动执行 compile 和 test）
mvn clean package

# 运行 Spring Boot 项目
cd junoyi-server
mvn spring-boot:run

# 查看依赖树
mvn dependency:tree

# 查看有效的 POM（解析后的）
mvn help:effective-pom

# 更新依赖
mvn clean install -U
```

---

## 四、创建新模块速查

### 1. 创建 API 模块

```bash
# 1. 创建目录
cd junoyi-module-api
mkdir junoyi-module-xxx-api

# 2. 创建 pom.xml（参考模板）

# 3. 在父 pom.xml 添加
<modules>
    <module>junoyi-module-xxx-api</module>
</modules>

# 4. 在 dependencies 中注册
<dependency>
    <groupId>com.junoyi</groupId>
    <artifactId>junoyi-module-xxx-api</artifactId>
    <version>${revision}</version>
</dependency>
```

### 2. 创建实现模块

```bash
# 1. 创建目录
cd junoyi-module
mkdir junoyi-module-xxx

# 2. 创建 pom.xml（参考模板）

# 3. 在父 pom.xml 添加
<modules>
    <module>junoyi-module-xxx</module>
</modules>

# 4. 在 dependencies 中注册
<dependency>
    <groupId>com.junoyi</groupId>
    <artifactId>junoyi-module-xxx</artifactId>
    <version>${revision}</version>
</dependency>

# 5. 在 server 中引入
<dependency>
    <groupId>com.junoyi</groupId>
    <artifactId>junoyi-module-xxx</artifactId>
</dependency>
```

---

## 五、代码模板速查

### Entity 实体类模板

```java
package com.junoyi.module.xxx.api.domain.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("表名")
public class XxxEntity {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String name;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
    
    @TableLogic
    private Integer deleted;
}
```

### VO 视图对象模板

```java
package com.junoyi.module.xxx.api.domain.vo;

import lombok.Data;
import java.time.LocalDateTime;

@Data
public class XxxVO {
    private Long id;
    private String name;
    private LocalDateTime createTime;
}
```

### Mapper 接口模板

```java
package com.junoyi.module.xxx.api.mapper;

import com.junoyi.module.xxx.api.domain.entity.XxxEntity;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface XxxMapper extends BaseMapper<XxxEntity> {
}
```

### Service 接口模板

```java
package com.junoyi.module.xxx.api.service;

import com.junoyi.module.xxx.api.domain.entity.XxxEntity;
import com.baomidou.mybatisplus.extension.service.IService;

public interface IXxxService extends IService<XxxEntity> {
}
```

### Service 实现模板

```java
package com.junoyi.module.xxx.service.impl;

import com.junoyi.module.xxx.api.domain.entity.XxxEntity;
import com.junoyi.module.xxx.api.mapper.XxxMapper;
import com.junoyi.module.xxx.api.service.IXxxService;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springframework.stereotype.Service;

@Service
public class XxxServiceImpl extends ServiceImpl<XxxMapper, XxxEntity> 
        implements IXxxService {
}
```

### Controller 模板

```java
package com.junoyi.module.xxx.controller;

import com.junoyi.module.xxx.api.service.IXxxService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/xxx")
public class XxxController {

    @Autowired
    private IXxxService xxxService;

    @GetMapping("/list")
    public Object list() {
        return xxxService.list();
    }

    @GetMapping("/{id}")
    public Object getById(@PathVariable Long id) {
        return xxxService.getById(id);
    }

    @PostMapping
    public Object save(@RequestBody Object entity) {
        return xxxService.save(entity);
    }

    @PutMapping
    public Object update(@RequestBody Object entity) {
        return xxxService.updateById(entity);
    }

    @DeleteMapping("/{id}")
    public Object delete(@PathVariable Long id) {
        return xxxService.removeById(id);
    }
}
```

### MapStruct 转换器模板

```java
package com.junoyi.module.xxx.convert;

import com.junoyi.module.xxx.api.domain.entity.XxxEntity;
import com.junoyi.module.xxx.api.domain.vo.XxxVO;
import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;
import java.util.List;

@Mapper
public interface XxxConvert {
    
    XxxConvert INSTANCE = Mappers.getMapper(XxxConvert.class);
    
    XxxVO toVO(XxxEntity entity);
    
    List<XxxVO> toVOList(List<XxxEntity> entities);
}
```

---

## 六、依赖关系速查

### 允许的依赖

```
✅ module-xxx → module-xxx-api
✅ module-xxx → framework-web
✅ module-xxx → common
✅ module-xxx-api → common
✅ framework-web → framework-core
✅ framework-core → common
✅ server → module-xxx
```

### 禁止的依赖

```
❌ module-xxx-api → framework-web
❌ module-xxx-api → module-xxx
❌ module-a → module-b (实现模块互相依赖)
❌ common → 任何内部模块
❌ 循环依赖
```

---

## 七、版本管理速查

### 修改整体版本

```xml
<!-- 只需修改根 pom.xml -->
<properties>
    <revision>1.1.0</revision>
</properties>
```

### 添加新的第三方依赖

```xml
<!-- 1. 在 junoyi-dependencies/pom.xml 定义版本 -->
<properties>
    <新依赖.version>x.x.x</新依赖.version>
</properties>

<!-- 2. 在 dependencyManagement 中声明 -->
<dependency>
    <groupId>xxx</groupId>
    <artifactId>xxx</artifactId>
    <version>${新依赖.version}</version>
</dependency>

<!-- 3. 在需要的模块中引用（不带版本号） -->
<dependency>
    <groupId>xxx</groupId>
    <artifactId>xxx</artifactId>
</dependency>
```

### 升级依赖版本

```xml
<!-- 只需修改 junoyi-dependencies/pom.xml 中的版本号 -->
<properties>
    <spring.boot.version>3.2.1</spring.boot.version>
</properties>
```

---

## 八、包名规范速查

```
com.junoyi.common                    # 公共模块
com.junoyi.framework.core            # 框架核心
com.junoyi.framework.web             # 框架Web
com.junoyi.module.xxx.api            # 模块API
com.junoyi.module.xxx                # 模块实现
```

---

## 九、常见问题速查

| 问题 | 原因 | 解决方案 |
|-----|------|---------|
| 找不到依赖 | 未在 dependencies 中声明 | 在 junoyi-dependencies 中添加 |
| 版本冲突 | 子模块硬编码版本 | 移除子模块的 version 标签 |
| 循环依赖 | 模块间循环引用 | 通过 API 模块解耦 |
| 构建失败 | 模块顺序错误 | 检查 modules 声明顺序 |
| 类找不到 | 未安装到本地仓库 | 执行 mvn clean install |

---

## 十、开发检查清单

创建新模块时检查：

- [ ] 在父模块 pom.xml 中添加 `<module>` 声明
- [ ] 在 junoyi-dependencies 中添加版本管理
- [ ] parent 正确指向父模块
- [ ] relativePath 设置为 `../pom.xml`
- [ ] 不要硬编码版本号
- [ ] 包名遵循规范
- [ ] API 模块只定义接口
- [ ] 实现模块依赖对应 API

---

## 十一、技术栈版本

| 技术 | 版本 | 说明 |
|-----|------|------|
| Java | 21 | JDK版本 |
| Spring Boot | 3.2.0 | 框架版本 |
| MyBatis Plus | 3.5.5 | ORM框架 |
| MySQL | 8.0.33 | 数据库 |
| Druid | 1.2.20 | 连接池 |
| Lombok | 1.18.30 | 代码简化 |
| Hutool | 5.8.23 | 工具类库 |
| FastJson2 | 2.0.43 | JSON处理 |
| Knife4j | 4.3.0 | 接口文档 |
| JWT | 0.12.3 | 认证 |
| MapStruct | 1.5.5.Final | 对象转换 |

---

**快速参考手册 - 随时查阅，高效开发！**
